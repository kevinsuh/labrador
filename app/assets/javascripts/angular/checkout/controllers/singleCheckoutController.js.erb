(function() {
	
	/**
	 * this is the controller used to handle checkout of the cards you queued
	 */
	var app = angular.module('card-queue').controller("SingleCheckoutController", function($rootScope, $scope) {
			
		$scope.selectedAddress = {id: 0};
		$scope.selectedPayment = {last4: 0};

	});

})();
