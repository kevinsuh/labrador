(function() {
	
	/**
	 * this controller is specific to the checkout. it will update based on how many cards have been queued thus far, and could potentially provide other awesome information as well
	 */
	var app = angular.module('card-queue').controller("CheckOutController", ['$scope', '$state', function($scope, $state) {
			
	}]);

})();

